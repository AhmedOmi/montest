#include "monty.h"
/**
  *
  *
  */
void push(int x)
{
	stack_t *newNode;

	newNode = top;
	if (newNode == NULL)
	{
		newNode == malloc(sizeof(stack_t));
		newNode->n = x;
		newNode->next = NULL;
		newNode ->prev =;
	}
	else
	{
	}
}
